# Project: AI Coding Workflow

## Project Context
Read `.workflow/project-overview.md` for project overview (generated by `/init`).

## Workflow Commands
- `/init` — Initialize project overview (run once on new project)
- `/plan` — Create execution plan from requirements
- `/execute` — Execute an approved plan (`--resume` to continue interrupted work)
- `/analyze` — Deep-analyze a component (`--recursive` for dependency tree)
- `/template-create` — Extract a repeatable pattern from completed work
- `/template-apply` — Use a template to accelerate new implementation
- `/doc-update` — Assess and apply documentation updates for changed components
- `/jira` — Fetch and transform a Jira ticket to clean markdown

## Component Knowledge
Component docs are co-located: `{ComponentName}.analysis.md` alongside the source.
To find all docs: glob `**/*.analysis.md`.
When working on a component, read its `.analysis.md` first. If it doesn't exist, run `/analyze` first.

## Integration Templates
Template index: `.workflow/templates/index.md`
When planning new features, check if an existing template matches before planning from scratch.
Templates capture repeatable patterns — use `/template-apply {name}` to accelerate implementation.

## Quality Rules
General quality rules (always loaded): `.claude/rules/`
Project-specific rules for plan review: `.workflow/rules/planning/`
Project-specific rules for code review: `.workflow/rules/code/`

## State & Resume
Current execution state: `.workflow/plans/{current}/state.md`
On session start, check for interrupted executions (`status: executing` or `paused` in state.md).
Resume with `/execute --resume`.

## Configuration
Project config: `.workflow/config.json`
